let controller = require("../controllers/service.js")

module.exports = (app) => {

    //app.get("/api/service", controller.getAllService)
    //app.get("/api/service/:id", controller.getService)
    //app.post("/api/service", controller.addService)
    //app.delete("/api/service/:id", controller.deleteService)
}